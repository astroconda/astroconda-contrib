{% set name = 'specviz' %}
{% set version = GIT_DESCRIBE_TAG ~ ".dev" ~ GIT_DESCRIBE_NUMBER ~ "+" ~ GIT_DESCRIBE_HASH %}
{% if version[0] == 'v' %}
{%   set version = version[1:] %}
{% endif %}
{% set number = '1' %}

package:
  name: {{ name }}
  version: {{ version }}

build:
  number: {{ number }}

source:
  git_rev: 27ee0ba596db834d6468d3da11ae8c6fff5f2281
  git_url: https://github.com/spacetelescope/specviz.git

requirements:
  build:
    - astropy
    - cython
    - docopt
    - specutils <=0.2.2
    - py-expression-eval
    - pyyaml
    - pyqtgraph >=0.9.11
    - pyqt
    - qtpy
    - scipy
    - spectral-cube
    - yaml
    - setuptools
    - six
    - numpy {{ numpy }}
    - python {{ python }}

  run:
    - astropy
    - cython
    - docopt
    - specutils <=0.2.2
    - py-expression-eval
    - pyyaml
    - pyqtgraph >=0.9.11
    - pyqt
    - qtpy
    - scipy
    - spectral-cube
    - yaml
    - six
    - numpy
    - python

test:
  requires:
    - astropy
    - specutils <=0.2.2
  imports:
    - specviz

about:
  home: https://github.com/spacetelescope/specviz
  license: BSD
  summary: An interactive astronomical 1D spectra analysis tool
