{% set name = 'dust_extinction' %}
{% set version = '0.7' %}
{% set number = '0' %}

about:
    home: https://github.com/karllark/{{ name }}
    license: BSD
    summary: |
        Provides models of interstellar dust extinction curves

build:
    number: {{ number }}

package:
    name: {{ name }}
    version: {{ version }}

requirements:
    build:
    - astropy >=1.3
    - pytest
    - setuptools
    - numpy {{ numpy }}
    - python {{ python }}
    run:
    - astropy >=1.3
    - matplotlib
    - scipy
    - numpy
    - python

source:
    git_tag: {{ version }}
    git_url: https://github.com/karllark/{{ name }}.git

test:
    imports:
    - dust_extinction
